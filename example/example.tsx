import { h, CustomComponent } from '../dist';

class MySecondComponent extends CustomComponent {
    template () {
        return (
            <div>
                Hello
            </div>
        );
    }
}